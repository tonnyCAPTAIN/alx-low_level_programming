#include <stdio.h>
#include "main.h"
/**
 * main- prints all arguments it receives
 * @argc: count argument
 * @argv: array argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int 1;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[i]);
	return (0);
}
